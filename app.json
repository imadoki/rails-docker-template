{
  "name": "Rails Docker Template",
  "addons": ["jawsdb:kitefin"],
  "stack": "container",
  "environments": {
    "review": {
      "addons": ["jawsdb:kitefin"],
      "scripts": {
        "postdeploy": "bundle exec rails db:schema:load db:seed"
      }
    }
  }
}
